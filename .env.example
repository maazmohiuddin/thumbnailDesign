# Only needed on static/serverless deployments (Vercel, Netlify, ...) where
# server/index.js is hosted separately. Leave unset for local `npm run dev`.
VITE_REEL_API_BASE_URL=https://your-reel-api-host.example.com
